syntax = "proto3";

package rpc;

option go_package = "kimpay-backend/internal/rpc";

service WalletService {
    rpc GetBalance (GetBalanceRequest) returns (GetBalanceResponse);
}

message GetBalanceRequest {
    uint64 wallet_id = 1;
}

message GetBalanceResponse {
    double balance = 1;
    string currency = 2;
}
